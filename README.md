
  # grad project

  This is a code bundle for grad project. The original project is available at https://www.figma.com/design/Vy2cavU5BVn43IfJVewQZF/grad-project.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  